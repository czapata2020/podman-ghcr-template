# .github/workflows/build-and-push.yml
#
# Construye y publica en GHCR las imágenes propias del proyecto.
# El nombre de usuario/org se toma solo de GitHub (github.repository_owner),
# así que aquí NO hay que poner tu usuario. Solo ajusta:
#   - {{PROJECT}}  -> nombre base del proyecto (lo hace setup.sh)
#   - la lista 'matrix.include' segun tus servicios (añade/quita bloques)
#
# Se dispara en cada push a main y en cada tag vX.Y.Z.

name: Build and Push Images

on:
  push:
    branches: [main]
    tags: ['v*']
  workflow_dispatch:

env:
  REGISTRY: ghcr.io

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        # Un bloque por imagen propia. Para la app multi-stage usa 'target'.
        # Servicios de un solo stage dejan target vacío.
        include:
          - name: app
            context: .
            dockerfile: ./Dockerfile
            target: runner          # stage de produccion (apps multi-stage)
          - name: db
            context: ./db
            dockerfile: ./db/Dockerfile
            target: ''
          - name: vault
            context: ./vault
            dockerfile: ./vault/Dockerfile
            target: ''
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Tags y labels (${{ matrix.name }})
        id: meta
        uses: docker/metadata-action@v5
        with:
          # ghcr.io/<owner>/{{PROJECT}}-<servicio>
          images: ${{ env.REGISTRY }}/${{ github.repository_owner }}/{{PROJECT}}-${{ matrix.name }}
          tags: |
            type=sha,format=short
            type=ref,event=tag
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push (${{ matrix.name }})
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.dockerfile }}
          target: ${{ matrix.target }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=${{ matrix.name }}
          cache-to: type=gha,mode=max,scope=${{ matrix.name }}
